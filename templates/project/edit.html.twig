{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <a class="btn btn-light my-3" href="{{ path('project_index') }}">Revenir à l'acceuil</a>  
    <h1 class="my-3">Editer le Projet</h1>

    {{ include('project/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a  type="button" href="{{ path('project_delete', {'id': project.id}) }}"  class="btn btn-danger my-3">Supprimer</a>
{% endblock %}
